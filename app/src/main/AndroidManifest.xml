<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.INTERNET"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projeto"
        tools:targetApi="33">

        <activity
            android:name=".ui.user.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.lists.SearchActivity" />

        <activity android:name=".ui.books.BookDetailsActivity" />

        <activity android:name=".ui.user.LoginActivity" />

        <activity android:name=".ui.user.CadastroActivity" />

        <activity android:name=".ui.lists.FavoritesActivity" />

        <activity android:name=".ui.user.AccountActivity" />

        <activity android:name=".ui.books.SavedBookActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>